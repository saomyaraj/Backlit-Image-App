name: backlit_flutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  tflite_flutter: ^0.10.4
  image_picker: ^1.1.2
  http: ^1.2.1
  firebase_ml_model_downloader: ^0.3.0
  # tflite_flutter_helper: ^0.2.0
  tflite_flutter_helper_plus: ^0.0.2
  image: ^3.0.2
  tflite_flutter_plus: ^0.0.1 # Add the appropriate version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/models/gen3_quantized.tflite